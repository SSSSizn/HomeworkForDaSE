#include "types.h"
#include "stat.h"
#include "user.h"

int main(int argc, char *argv[])
{

    int number = atoi(argv[1]);
    settickets(number);

    while (1)
    {
        /* code */
        /* just for delay */
    }

    exit();
}